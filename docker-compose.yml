services:
  db:
    image: gvenzl/oracle-xe:21-slim  # Otra opción gvenzl/oracle-free!
    container_name: encuentros_db
    environment:
      ORACLE_PASSWORD: admin
      ORACLE_DATABASE: XEPDB1
    ports:
      - "1521:1521"
    volumes:
      - oracle_data:/opt/oracle/oradata
    restart: always

  backend:
    build:
      context: ./encuentros-back
    container_name: encuentros_back
    environment:
      DB_HOST: db
      DB_PORT: "1521"
      DB_USERNAME: ENCUENTROS_ADMIN
      DB_PASSWORD: admin
      DB_SERVICE_NAME: XEPDB1
      PORT: "3000"
      ORACLE_SYS_PASSWORD: admin
    ports:
      - "3000:3000"
    volumes:
      - ./init-db:/app/init-db
    depends_on:
      - db
    restart: on-failure

  frontend:
    build:
      context: ./encuentros-front
    container_name: encuentros_front
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: on-failure

volumes:
  oracle_data:
